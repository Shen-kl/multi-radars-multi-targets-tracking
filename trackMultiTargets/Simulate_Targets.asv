%仿真目标轨迹
s = sprintf("====Simulate_Targets====");
disp(s);

%加载目标轨迹
load("target.mat");
target_num = 4;%要加载的目标数量

for index = 1 : target_num
    exp = ['target{' num2str(index) '}=target' num2str(index)];
    eval(exp);
end

